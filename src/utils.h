#ifndef UTILS
#define UTILS

#include <array>
#include <vector>
#include <string>
#include <sstream>

using namespace std;

//returns a vector of strings which were extracted from the given input string by cutting it at each given delimiter character within
vector<string> cutStringAtDelimiter(string, char);

#endif